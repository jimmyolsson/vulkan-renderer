[package]
name = "vulkanrust"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.101"
ash = { version = "0.38", default-features = false, features = ["linked", "debug", "std"] }
ash-window = "0.13.0"
winit = { version = "0.29", features = ["rwh_06"] }
